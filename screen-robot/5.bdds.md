# BDDs — screen-robot

**Por quê:** aceite Gherkin (US/SC), fonte [`4.scenarios.md`](4.scenarios.md).  
**Épicos:** [`2.epics.md`](2.epics.md) · **Cenários:** [`4.scenarios.md`](4.scenarios.md).  
**Visão:** [`README.md`](README.md).  
**Piloto:** [`src/scripts/linkedin-login.js`](src/scripts/linkedin-login.js) — limpa screenshots → `resetInstance` → provision → LinkedIn → print tela inicial → `findByText("Sign in with Email")` → tap `center` → lista OCR no console (sem digitar credenciais / sem `saveSession`).  
**Aceite oficial OCR partido:** **SC-30** — fixture [`linkedin-tela-inicial.png`](src/test/fixtures/linkedin-tela-inicial.png) · BDD [`sc-30-linkedin-sign-in-with-email.test.js`](src/test/bdd/sc-30-linkedin-sign-in-with-email.test.js).

```bash
cd screen-robot/src
node --test --test-timeout=120000 test/bdd/sc-30-linkedin-sign-in-with-email.test.js
node --test --test-timeout=120000 lib/find-by-text.fixture.test.js
npm run linkedin-login   # piloto ao vivo
```

Cada bloco: Dado / Quando / Então alinhado a Entradas / Execução / Saídas.

---

## EP-01 — Provisionar agente

**US:** US-01 · US-20 · US-22 · **SC:** SC-01..03 · SC-25..26 · SC-28 · **Cenários:** [`4.scenarios.md#ep-01--provisionar-agente`](4.scenarios.md#ep-01--provisionar-agente) · **Plano:** [`implementation-plan/EP-01-provisionar-agente.md`](implementation-plan/EP-01-provisionar-agente.md)

### US-01 — Provisionar um agente

```gherkin
Cenário: US-01 Agent nomeado fica pronto para ADB
  Dado o config com nome único e o runtime Android disponíveis
  Quando o provisionamento cria um AVD/emulador novo para esse nome, garante serial online e aguarda boot completo
  Então o agent está pronto para ADB (nome, serial online e boot ok)
```

#### SC-01 — Criar AVD/emulador nomeado

```gherkin
Cenário: SC-01 AVD/emulador novo sobe e fica alcançável
  Dado nome único do agent, host e AVD/runtime
  Quando a lib aloca serial e sobe um AVD/emulador novo ligado ao nome
  Então o agent nomeado está em execução e alcançável
```

#### SC-02 — Garantir serial ADB online

```gherkin
Cenário: SC-02 Serial ADB fica online
  Dado o agent alcançável e o serial alocado ao nome
  Quando adb connect / listagem de devices é repetida até o serial aparecer como device
  Então o serial ADB está online
```

#### SC-03 — Aguardar boot completo

```gherkin
Cenário: SC-03 Boot completo no device
  Dado o serial ADB online
  Quando o sistema faz poll de boot (ex. sys.boot_completed)
  Então o device reporta boot completo
```

### US-20 — Resgatar agente existente

```gherkin
Cenário: US-20 Agent existente é resgatado pelo nome
  Dado um agent já provisionado com determinado nome
  Quando provisionEmulator é chamado de novo com o mesmo nome
  Então o handle fica pronto sem criar outro emulador
```

#### SC-25 — Localizar agent pelo nome

```gherkin
Cenário: SC-25 Runtime é resolvido pelo nome
  Dado o nome de um agent já criado
  Quando a lib resolve AVD/emulador e serial associados ao nome
  Então a referência ao runtime existente está disponível
  # se o nome não existir → erro tipado (ex. PROVISION_NAME_NOT_FOUND)
```

#### SC-26 — Reconectar e confirmar boot

```gherkin
Cenário: SC-26 Handle anexado fica pronto
  Dado o runtime localizado pelo nome
  Quando adb connect / wait-for-device e poll de boot concluem
  Então o handle está pronto (serial online, boot ok) sem novo AVD/emulador
```

### US-22 — Mascarar identidade do aparelho

```gherkin
Cenário: US-22 Apps veem aparelho de mercado, não o vendor de automação
  Dado um agent com boot completo em qualquer vendor de runtime
  Quando as props de produto do Android são lidas (ex. ro.product.model)
  Então os valores são de aparelho de mercado e não expõem o fingerprint do vendor
```

#### SC-28 — Identidade de aparelho de mercado

```gherkin
Cenário: SC-28 Identidade de aparelho de mercado
  Dado agent boot ok e perfil de produto configurado no vendor
  Quando getprop (ou equivalente) de ro.product.* é consultado
  Então brand/model/device são de aparelho comum e sem nome do runtime de automação
```

---

## EP-02 — Eventos de UI

**US:** US-02..05 · **SC:** SC-04..07 · **Cenários:** [`4.scenarios.md#ep-02--eventos-de-ui`](4.scenarios.md#ep-02--eventos-de-ui) · **Plano:** [`implementation-plan/EP-02-eventos-de-ui.md`](implementation-plan/EP-02-eventos-de-ui.md)

### US-02 — Evento de boot

```gherkin
Cenário: US-02 Boot do device é sinalizado
  Dado o serial online
  Quando o sistema espera o sinal de boot
  Então o boot é sinalizado
```

#### SC-04 — Sinal de boot é recebido

```gherkin
Cenário: SC-04 Sinal de boot é recebido
  Dado o serial online
  Quando o listener aguarda o evento de boot
  Então o boot é sinalizado
```

### US-03 — Evento de app aberta

```gherkin
Cenário: US-03 App aberta é confirmada
  Dado o package em foreground esperado
  Quando o sistema espera a app em foreground
  Então a app aberta está confirmada
```

#### SC-05 — App em foreground é confirmada

```gherkin
Cenário: SC-05 App em foreground é confirmada
  Dado o package esperado em foreground
  Quando o sistema aguarda a app aberta
  Então a app está em foreground
```

### US-04 — Evento de tela estável

```gherkin
Cenário: US-04 Tela estável é confirmada
  Dado a app em foreground e fonte de frame (screenshot/stream/câmera)
  Quando o sistema espera frame estável (hash/diff de imagem sem transição)
  Então a tela está estável
```

#### SC-06 — Tela fica estável

```gherkin
Cenário: SC-06 Tela fica estável
  Dado a app em foreground e pipeline de frame
  Quando o hash/diff do frame permanece estável por stableMs
  Então a tela está estável
```

### US-05 — Evento de mudança de frame

```gherkin
Cenário: US-05 Frame atualizado fica disponível
  Dado um frame anterior (opcional) e fonte de imagem online
  Quando o sistema detecta mudança no frame (hash/diff visual)
  Então um frame atualizado está disponível
```

#### SC-07 — Frame da tela muda

```gherkin
Cenário: SC-07 Frame da tela muda
  Dado um frame anterior (ou ausência) e fonte de imagem
  Quando o frame muda em relação ao anterior (hash/diff visual)
  Então um frame atualizado está disponível
```

---

## EP-03 — Instalar APKs

**US:** US-06 · **SC:** SC-08..10 · **Cenários:** [`4.scenarios.md#ep-03--instalar-apks`](4.scenarios.md#ep-03--instalar-apks) · **Plano:** [`implementation-plan/EP-03-instalar-apks.md`](implementation-plan/EP-03-instalar-apks.md)

### US-06 — Instalar APKs

```gherkin
Cenário: US-06 Apps da config ficam instalados na versão definida
  Dado o agent provisionado e a lista de apps/versões na config
  Quando cada pacote é lido, baixado e instalado
  Então os apps estão instalados nas versões definidas
```

#### SC-08 — Ler versão na config do dispositivo

```gherkin
Cenário: SC-08 Versão e package são lidos da config
  Dado o arquivo device.config.json
  Quando apps.*.version e package são parseados
  Então a versão e o package alvo estão disponíveis para download
```

#### SC-09 — Baixar APK na versão definida

```gherkin
Cenário: SC-09 APK da versão pedida é baixado
  Dado package, versão e ferramenta de download (ex. apkeep)
  Quando o download da versão definida é executado
  Então o artefato APK/XAPK existe no disco
```

#### SC-10 — Instalar pacote no agent

```gherkin
Cenário: SC-10 Pacote é instalado no agent
  Dado serial online e caminho do APK
  Quando adb install (ou equivalente) é executado
  Então o pacote está instalado no agent
```

---

## EP-04 — Operar tela

**US:** US-07..12 · US-21 · **SC:** SC-11..16 · SC-27 · **Cenários:** [`4.scenarios.md#ep-04--operar-tela`](4.scenarios.md#ep-04--operar-tela) · **Plano:** [`implementation-plan/EP-04-operar-tela.md`](implementation-plan/EP-04-operar-tela.md)

### US-07 — Abrir aplicativo

```gherkin
Cenário: US-07 App fica em foreground no agent
  Dado package (e activity opcional)
  Quando o launch do app é executado no agent
  Então a app está em foreground
```

#### SC-11 — App é aberta no agent

```gherkin
Cenário: SC-11 App é aberta no agent
  Dado package (e activity opcional)
  Quando launch é executado
  Então a app está em foreground
```

### US-08 — tap

```gherkin
Cenário: US-08 UI reflete o toque
  Dado coordenadas x,y ou bounds vindos de visão/OCR sobre o frame
  Quando o toque na tela é executado
  Então a UI reflete o tap
```

#### SC-12 — Toque na tela

```gherkin
Cenário: SC-12 Toque na tela
  Dado coordenadas x,y ou bounds vindos de visão/OCR sobre o frame
  Quando tap é executado nesse alvo
  Então a UI reflete o toque
```

### US-09 — type

```gherkin
Cenário: US-09 Texto aparece na UI
  Dado um texto e o teclado visível no frame (região opcional x,y,width,height)
  Quando type localiza as teclas via OCR e toca cada uma
  Então o texto aparece na UI
```

#### SC-13 — Texto é digitado

```gherkin
Cenário: SC-13 Texto é digitado
  Dado um texto e o teclado visível no frame (região opcional x,y,width,height)
  Quando type faz OCR das teclas e digita só com tap em cada tecla
  Então o texto aparece na UI
```

### US-10 — scroll

```gherkin
Cenário: US-10 Conteúdo da tela ou lista é rolado
  Dado direção (up/down/left/right), distância ou bounds da área
  Quando swipe / scroll é executado
  Então o conteúdo rolou e novos itens podem ficar visíveis
```

#### SC-14 — Conteúdo é rolado

```gherkin
Cenário: SC-14 Conteúdo é rolado
  Dado direção, distância ou bounds da área
  Quando scroll/swipe é executado
  Então o conteúdo rolou e novos itens podem ficar visíveis
```

### US-11 — Capturar frame (screenshot / câmera)

```gherkin
Cenário: US-11 Frame da tela é capturado
  Dado fonte de frame (screencap ADB, stream ou câmera) e path de saída
  Quando o frame da tela é capturado
  Então o arquivo de imagem existe
```

#### SC-15 — Frame da tela é salvo

```gherkin
Cenário: SC-15 Frame da tela é salvo
  Dado fonte de frame e path de saída
  Quando o frame é capturado (screenshot ADB ou câmera)
  Então o arquivo de imagem existe no path
```

### US-12 — Resgatar coordenadas x,y a partir de uma imagem

```gherkin
Cenário: US-12 Coordenadas do alvo são devolvidas
  Dado uma imagem de entrada e o frame/tela atual
  Quando o match por visão/template é executado
  Então as coordenadas x,y (e confiança) são devolvidas
```

#### SC-16 — Coordenadas a partir de imagem template

```gherkin
Cenário: SC-16 Coordenadas a partir de imagem template
  Dado uma imagem de entrada (template) e o frame/tela atual
  Quando o match por visão/template é executado
  Então coordenadas x,y (e confiança) são devolvidas
```

### US-21 — Abrir scrcpy (espelhar tela)

```gherkin
Cenário: US-21 Tela do agent fica espelhada no scrcpy
  Dado um handle com serial ADB online e scrcpy no PATH
  Quando handle.openScrcpy é chamado
  Então a janela scrcpy abre ligada ao serial e a tela fica operável (tap, digitar)
```

#### SC-27 — Espelhamento scrcpy aberto

```gherkin
Cenário: SC-27 Espelhamento scrcpy aberto
  Dado handle provisionado (serial online) e scrcpy instalado
  Quando openScrcpy é executado no handle
  Então o processo/janela scrcpy fica ativo no serial do agent
```

---

## EP-05 — Extrair elementos

**US:** US-13 · US-23 (US-14..16 **cancelados**) · **SC:** SC-17 · SC-29..30 (SC-18..21 **cancelados**) · **Cenários:** [`4.scenarios.md#ep-05--extrair-elementos`](4.scenarios.md#ep-05--extrair-elementos) · **Plano:** [`implementation-plan/EP-05-extrair-elementos.md`](implementation-plan/EP-05-extrair-elementos.md)

### US-13 — Extrair elementos (OCR)

```gherkin
Cenário: US-13 Lista plana só com textos OCR
  Dado uma imagem/frame da tela (screenshot ADB, stream ou câmera)
  Quando extract() roda OCR sobre o frame
  Então a lista devolvida contém apenas elementos type "text"
  E não contém icon, list, image nem other
```

#### SC-17 — Extrair textos

```gherkin
Cenário: SC-17 Lista de elementos com textos
  Dado uma imagem/frame da tela
  Quando OCR extrai textos + bounds
  Então a lista de elementos type "text" é devolvida
```

#### SC-18 — Enriquecer lista (fase 2) — **cancelado / removido**

Não faz parte do produto.

### US-14 — Extrair ícones — **cancelado / removido**

Não faz parte do produto.

#### SC-19 — Extrair ícones — **cancelado / removido**

### US-15 — Extrair listas — **cancelado / removido**

Não faz parte do produto.

#### SC-20 — Extrair listas — **cancelado / removido**

### US-16 — Extrair imagens — **cancelado / removido**

Não faz parte do produto.

#### SC-21 — Extrair imagens — **cancelado / removido**

### US-23 — Buscar elemento(s) por texto (similaridade)

```gherkin
Cenário: US-23 Busca por texto encontra elementos lado a lado na string maior
  Dado um agent com serial e tela visível
  E um texto-alvo (string maior) "Sign in with Email"
  Quando findByText(serial, query) é chamado
  Então extractElements roda internamente (OCR)
  E devolve só elementos lado a lado cujo texto unido está contido na string maior
  E o score de similaridade é elevado (≥ limiar)
```

#### SC-29 — Buscar por texto com score

```gherkin
Cenário: SC-29 Match de elementos adjacentes dentro da query
  Dado serial e texto-alvo (string maior)
  Quando findByText encapsula extractElements e ranqueia matches
  Então o resultado inclui elementos lado a lado
  E o texto unido desses elementos está contido na string maior
  E o caller não passa a lista de elementos
```

#### SC-30 — LinkedIn — Sign in with Email

```gherkin
Cenário: SC-30 LinkedIn Sign in with Email (OCR partido)
  Dado o print inicial do LinkedIn (fixture linkedin-tela-inicial.png)
  E o texto-alvo "Sign in with Email" (string maior)
  Quando findByText é chamado com a fixture como frame
  Então retorna os elementos OCR lado a lado ["Sign", "in", "with", "Email"]
  E cada parte está contida na string maior
  E o score de similaridade é elevado
  E o center está disponível para tap
```

---

## EP-06 — Sessão

**US:** US-17..19 · **SC:** SC-22..24 · **Cenários:** [`4.scenarios.md#ep-06--sessao`](4.scenarios.md#ep-06--sessao) · **Plano:** [`implementation-plan/EP-06-sessao.md`](implementation-plan/EP-06-sessao.md)

### US-17 — Salvar sessão

```gherkin
Cenário: US-17 Sessão é gravada em disco
  Dado estado em memória e path da sessão
  Quando a serialização grava o arquivo
  Então o arquivo de sessão existe
```

#### SC-22 — Salvar sessão

```gherkin
Cenário: SC-22 Sessão é gravada
  Dado estado em memória e path da sessão
  Quando a serialização grava o arquivo
  Então o arquivo de sessão existe
```

### US-18 — Remover sessão

```gherkin
Cenário: US-18 Sessão é removida
  Dado um path de sessão
  Quando a remoção da sessão é executada
  Então o arquivo de sessão não existe
  E o runtime não mantém o contexto daquela sessão
```

#### SC-23 — Remover sessão

```gherkin
Cenário: SC-23 Sessão é removida do disco
  Dado um path de sessão
  Quando o arquivo de sessão é apagado
  Então o arquivo de sessão não existe
  E o runtime não mantém o contexto daquela sessão
```

### US-19 — Recuperar sessão

```gherkin
Cenário: US-19 Sessão é recuperada
  Dado um arquivo de sessão existente
  Quando a leitura reaplica o contexto
  Então o runtime possui o estado restaurado
```

#### SC-24 — Recuperar sessão

```gherkin
Cenário: SC-24 Sessão é recuperada
  Dado um arquivo de sessão existente
  Quando a leitura reaplica o contexto
  Então o runtime possui o estado restaurado
```

---

## Próximos passos

→ [`7.tasks.md`](7.tasks.md) · [`implementation-plan/`](implementation-plan/README.md)
