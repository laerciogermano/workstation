# POCs — screen-robot

Provas de conceito e runtimes auxiliares (não são a API Node).

| POC | Descrição |
|-----|-----------|
| [`redroid/`](redroid/README.md) | Android em Docker/Colima — **1 container por `name`**; identidade mascarada (não-redroid); `reset.sh` / `resetInstance` |
| [`android-studio/`](android-studio/README.md) | Emulador oficial (AVD) |

Código do robô: [`../src/`](../src/README.md).
