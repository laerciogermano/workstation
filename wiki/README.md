# Flow Wiki

Wiki HTML espelhando a estrutura de `flow/epics` e `flow/tasks`.

## Abrir

Abra [`index.html`](index.html) no navegador.

## Regenerar

Após alterar os Markdown em `flow/`:

```bash
python wiki/build.py
```
